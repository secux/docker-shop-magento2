# Magento2 shopsystem data-container

**Must implement:**

* list.sh
* install.sh

**IMPORTANT: That images should never RUN as container - just included as data-storage and helper for a special shopsystem to docker-shop-manager**